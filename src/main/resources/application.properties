spring.application.name=ShopCosmetic

# Setup server port
server.port=8080

# Database config
spring.datasource.url=jdbc:h2:file:./db/blogdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=minhdien
spring.datasource.password=28112002
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect


# Setup local h2 database console
spring.h2.console.enabled=true
spring.h2.console.path=/db-console
spring.h2.console.settings.web-allow-others=false

# Local h2 table setup on startup; use the value "none" in production!
spring.jpa.hibernate.ddl-auto=create-drop

# Enable lazy loading
spring.jpa.properties.hibernate.enable_lazy_load_no_trans = true
